#include<iostream>
using namespace std;
int n, q, link[100001], v[100001];
int main(void) {
	cin.tie(0);
	ios::sync_with_stdio(0);
	cin >> n >> q;
	for (int i = 1; i <= n; i++)cin >> v[i];

	return 0;
}