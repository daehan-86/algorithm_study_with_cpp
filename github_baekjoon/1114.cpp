#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int l, n, m,a[10001];
vector<int>v;
int main(void) {
	cin.tie(0);
	ios::sync_with_stdio(0);
	cin >> l >> n >> m;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	sort(a, a + n);
	for (int i = 0; i < n; i++) {

	}
	return 0;
}