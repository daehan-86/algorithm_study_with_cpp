#include<iostream>
#include<string>
using namespace std;
int T,n,t;
int main(void) {
	cin.tie(0);
	ios::sync_with_stdio(0);
	cin >> T;
	while (T--) {
		cin >> n >> t;
		vector<
		for (int i = 0; i < n; i++) {

		}
	}
	return 0;
}